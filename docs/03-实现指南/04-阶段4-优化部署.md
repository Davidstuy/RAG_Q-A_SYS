# 实现指南 - 阶段4：优化与部署

## 目标
流式输出、错误处理、性能优化、Docker 部署

---

## 1. 流式输出实现

### 添加依赖
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-webflux</artifactId>
</dependency>
```

### 更新 ChatService
```java
import org.springframework.web.servlet.mvc.method.annotation.SseEmitter;
import reactor.core.publisher.Flux;

public SseEmitter streamChat(ChatRequest request, String sessionId) {
    SseEmitter emitter = new SseEmitter(30000L);  // 30秒超时
    
    CompletableFuture.runAsync(() -> {
        try {
            // 1. 检索文档
            List<EmbeddingMatch<TextSegment>> docs = retrieveRelevantDocs(
                request.getQuestion(), 
                request.getTopK()
            );
            
            // 2. 构建上下文
            String context = buildContext(docs);
            
            // 3. 流式生成回答
            TokenStream tokenStream = chatLanguageModel.generate(
                List.of(SystemMessage.from(buildPrompt(request.getQuestion(), context)))
            );
            
            // 4. 逐字符发送
            StringBuilder fullAnswer = new StringBuilder();
            tokenStream.onNext(token -> {
                fullAnswer.append(token.content());
                emitter.send(SseEmitter.event().data(token.content()));
            });
            
            tokenStream.onComplete(() -> {
                // 保存对话历史
                historyService.save(sessionId, "user", request.getQuestion());
                historyService.save(sessionId, "assistant", fullAnswer.toString());
                emitter.complete();
            });
            
            tokenStream.onError(emitter::completeWithError);
            
        } catch (Exception e) {
            emitter.completeWithError(e);
        }
    });
    
    return emitter;
}
```

### 更新 Controller
```java
@GetMapping(value = "/stream", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
public SseEmitter streamChat(
    @RequestParam String question,
    @RequestParam(required = false) String sessionId
) {
    if (sessionId == null || sessionId.isBlank()) {
        sessionId = UUID.randomUUID().toString();
    }
    
    ChatRequest request = new ChatRequest();
    request.setQuestion(question);
    
    return chatService.streamChat(request, sessionId);
}
```

---

## 2. 错误处理

### 创建全局异常处理器
```java
package com.ragqa.exception;

import lombok.extern.slf4j.Slf4j;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

import java.util.HashMap;
import java.util.Map;

@Slf4j
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(IllegalArgumentException.class)
    public ResponseEntity<?> handleIllegalArgument(IllegalArgumentException e) {
        log.warn("参数错误: {}", e.getMessage());
        return ResponseEntity.badRequest().body(errorResponse("参数错误", e.getMessage()));
    }
    
    @ExceptionHandler(RuntimeException.class)
    public ResponseEntity<?> handleRuntime(RuntimeException e) {
        log.error("运行时错误", e);
        return ResponseEntity.internalServerError()
            .body(errorResponse("服务器错误", "请联系管理员"));
    }
    
    @ExceptionHandler(Exception.class)
    public ResponseEntity<?> handleException(Exception e) {
        log.error("未知错误", e);
        return ResponseEntity.internalServerError()
            .body(errorResponse("系统错误", "请联系管理员"));
    }
    
    private Map<String, Object> errorResponse(String error, String message) {
        Map<String, Object> response = new HashMap<>();
        response.put("error", error);
        response.put("message", message);
        response.put("timestamp", System.currentTimeMillis());
        return response;
    }
}
```

### 自定义异常
```java
package com.ragqa.exception;

public class DocumentParseException extends RuntimeException {
    public DocumentParseException(String message) {
        super(message);
    }
}

public class OpenAiApiException extends RuntimeException {
    public OpenAiApiException(String message) {
        super(message);
    }
}
```

---

## 3. 性能优化

### 3.1 添加缓存
```java
import org.springframework.cache.annotation.Cacheable;
import org.springframework.cache.annotation.EnableCaching;

@EnableCaching
@SpringBootApplication
public class RagQaApplication { }

@Service
public class DocumentService {
    
    @Cacheable(value = "embeddings", key = "#text.hashCode()")
    public float[] embed(String text) {
        return embeddingModel.embed(text).content();
    }
}
```

### 3.2 批量处理
```java
public void processDocumentsBatch(List<MultipartFile> files) {
    List<TextSegment> allSegments = new ArrayList<>();
    
    // 1. 提取所有文本
    for (MultipartFile file : files) {
        String text = extractText(file);
        Document document = Document.from(text);
        allSegments.addAll(splitter.split(document));
    }
    
    // 2. 批量向量化
    List<Embedding> embeddings = embeddingModel.embedAll(allSegments).content();
    
    // 3. 批量存储
    embeddingStore.addAll(embeddings, allSegments);
}
```

### 3.3 异步处理
```java
@Async
public CompletableFuture<DocumentDTO> processDocumentAsync(MultipartFile file) {
    DocumentDTO result = processDocument(file);
    return CompletableFuture.completedFuture(result);
}
```

---

## 4. 配置优化

### application-prod.yml
```yaml
server:
  port: 8080
  tomcat:
    threads:
      max: 200
      min-spare: 10

spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  
  cache:
    type: redis
    redis:
      time-to-live: 3600000  # 1小时

  jackson:
    default-property-inclusion: non_null

openai:
  api-key: ${OPENAI_API_KEY}
  base-url: ${OPENAI_BASE_URL}
  timeout:
    connect: 10s
    read: 60s

logging:
  level:
    com.ragqa: INFO
    dev.langchain4j: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when_authorized
```

---

## 5. Docker 部署

### Dockerfile
```dockerfile
FROM eclipse-temurin:17-jdk-alpine

WORKDIR /app

# 复制 Maven 文件
COPY pom.xml .
COPY src ./src

# 构建
RUN apk add --no-cache maven && \
    mvn clean package -DskipTests && \
    mv target/*.jar app.jar

# 运行
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

### docker-compose.yml
```yaml
version: '3.8'

services:
  rag-qa:
    build: .
    ports:
      - "8080:8080"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL}
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    
  # 可选：添加 Redis
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped

volumes:
  redis-data:
```

### 构建和运行
```bash
# 构建镜像
docker build -t rag-qa-system .

# 运行
docker run -d \
  -p 8080:8080 \
  -e OPENAI_API_KEY=your-key \
  rag-qa-system

# 或使用 docker-compose
docker-compose up -d
```

---

## 6. 健康检查和监控

### 添加 Actuator
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>
```

### 自定义健康检查
```java
@Component
public class OpenAiHealthIndicator implements HealthIndicator {
    
    @Autowired
    private ChatLanguageModel chatModel;
    
    @Override
    public Health health() {
        try {
            chatModel.generate("test");
            return Health.up()
                .withDetail("service", "OpenAI")
                .withDetail("status", "connected")
                .build();
        } catch (Exception e) {
            return Health.down()
                .withDetail("service", "OpenAI")
                .withDetail("error", e.getMessage())
                .build();
        }
    }
}
```

---

## 7. 测试

### 单元测试
```java
@SpringBootTest
class ChatServiceTest {
    
    @Autowired
    private ChatService chatService;
    
    @Test
    void testChat() {
        ChatRequest request = new ChatRequest();
        request.setQuestion("什么是 Spring Boot？");
        
        ChatResponse response = chatService.chat(request, "test-session");
        
        assertNotNull(response);
        assertNotNull(response.getAnswer());
        assertFalse(response.getSources().isEmpty());
    }
}
```

### 集成测试
```java
@SpringBootTest(webEnvironment = WebEnvironment.RANDOM_PORT)
@AutoConfigureMockMvc
class ChatControllerTest {
    
    @Autowired
    private MockMvc mockMvc;
    
    @Test
    void testChatEndpoint() throws Exception {
        String requestBody = """
            {
                "question": "什么是 Spring Boot？"
            }
            """;
        
        mockMvc.perform(post("/api/chat")
                .contentType(MediaType.APPLICATION_JSON)
                .content(requestBody))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.answer").isNotEmpty());
    }
}
```

---

## 8. 部署检查清单

- [ ] 配置正确的环境变量
- [ ] 设置合理的资源限制（CPU、内存）
- [ ] 配置日志收集
- [ ] 设置监控和告警
- [ ] 配置备份策略
- [ ] 准备回滚方案
- [ ] 性能测试
- [ ] 安全扫描

---

## 常见问题

### Q1: Docker 镜像太大？
**A:** 使用多阶段构建
```dockerfile
# 构建阶段
FROM maven:3.8-eclipse-temurin-17 AS builder
WORKDIR /app
COPY pom.xml .
COPY src ./src
RUN mvn clean package -DskipTests

# 运行阶段
FROM eclipse-temurin:17-jre-alpine
WORKDIR /app
COPY --from=builder /app/target/*.jar app.jar
ENTRYPOINT ["java", "-jar", "app.jar"]
```

### Q2: OpenAI API 限流？
**A:** 添加重试机制
```java
@Retryable(maxAttempts = 3, backoff = @Backoff(delay = 1000))
public String generateAnswer(String prompt) {
    return chatModel.generate(prompt);
}
```

---

## 恭喜！🎉

你已经完成了一个完整的 RAG 智能问答系统！

### 项目成果
- ✅ 文档上传与解析
- ✅ 向量化存储
- ✅ 语义检索
- ✅ LLM 问答
- ✅ 对话历史管理
- ✅ 流式输出
- ✅ 错误处理
- ✅ 性能优化
- ✅ Docker 部署

### 后续可以探索
- 集成 Milvus/ChromaDB
- 添加用户认证
- 支持更多文档格式
- 添加前端界面
- 集成国内 LLM（通义千问、文心一言）
- 添加文档管理和编辑功能

祝学习顺利！🚀